// build.gradle (raíz)
buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.1.0' // Plugin Android
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}