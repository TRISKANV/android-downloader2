pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    // Cambiamos a PREFER_SETTINGS y forzamos los repositorios aquí
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    repositories {
        google()
        mavenCentral()
        // Agregamos JitPack de forma completa
        maven { 
            url 'https://jitpack.io' 
            content {
                includeGroup "com.github.TeamNewPipe"
            }
        }
    }
}
rootProject.name = "android-downloader2"
include ':app'
