// Este archivo queda vacío o con este bloque para que no choque con settings.gradle
buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.0.0'
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:1.8.10"
    }
}

allprojects {
    // No agregar repositorios aquí, ya están en settings.gradle
}
